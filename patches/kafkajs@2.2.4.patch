diff --git a/src/network/requestQueue/index.js b/src/network/requestQueue/index.js
index eee14d66c3869d90f7bac0ac755ecbad3f2a2741..d9b3ee2655fc3ba20941b88d261c757704891e56 100644
--- a/src/network/requestQueue/index.js
+++ b/src/network/requestQueue/index.js
@@ -314,10 +314,13 @@ module.exports = class RequestQueue extends EventEmitter {
       if (this.pending.length > 0) {
         scheduleAt = scheduleAt > 0 ? scheduleAt : CHECK_PENDING_REQUESTS_INTERVAL
       }
+      // PATCH: Ensure timeout is always positive for Bun runtime compatibility
+      // See: https://github.com/oven-sh/bun/issues/6429
+      const safeTimeout = Math.max(1, scheduleAt)
       this.throttleCheckTimeoutId = setTimeout(() => {
         this.throttleCheckTimeoutId = null
         this.checkPendingRequests()
-      }, scheduleAt)
+      }, safeTimeout)
     }
   }
 }
diff --git a/src/utils/sleep.js b/src/utils/sleep.js
index a715e10cda66d7d0869b42f0250ce8b4679a598f..622528ff7e98ad0a9cfa9eea8edc5cf2bad2050a 100644
--- a/src/utils/sleep.js
+++ b/src/utils/sleep.js
@@ -1,4 +1,6 @@
 module.exports = timeInMs =>
   new Promise(resolve => {
-    setTimeout(resolve, timeInMs)
+    // PATCH: Ensure timeout is always positive for Bun runtime compatibility
+    const safeTime = Math.max(1, timeInMs)
+    setTimeout(resolve, safeTime)
   })
